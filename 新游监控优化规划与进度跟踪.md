# 新游监控优化规划与进度跟踪

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档目的 | 将项目 Review 结论转化为可执行、可验收、可持续跟踪的优化计划 |
| 建立日期 | 2026-08-28 |
| 当前版本 | v1.1 |
| 当前阶段 | 第一阶段：数据安全、聚合口径与采集可靠性 |
| 总体状态 | 第一阶段实现完成，待生产灰度验收 |

状态统一使用以下枚举：

- `未开始`：尚未进入实施。
- `进行中`：正在开发或迁移。
- `阻塞`：存在外部依赖、数据冲突或方案待确认。
- `待验收`：实现完成，等待测试、数据核对或生产观察。
- `已完成`：代码、测试、部署和生产观察均达到验收条件。

## 2. 优化目标与原则

### 2.1 总体目标

1. 任一采集、补全、目录重建或发布步骤失败时，不丢失线上用户数据，不留下半导入状态。
2. 单渠道或单产品失败时，正常渠道和正常产品仍可发布；失败部分保留旧数据并进入重试或隔离队列。
3. “采集成功”必须能够说明覆盖范围、完整程度和数据新鲜度，而不只是返回了非空列表。
4. 产品聚合口径与渠道聚合口径稳定、明确，不再因为事件类型不同产生重复卡片。
5. 产品身份逐步从可变名称迁移到稳定产品 ID，支持改名、活动后缀、渠道包和同名不同产品。
6. 所有优化都必须具备自动化测试、故障回滚、指标和生产验收证据。

### 2.2 实施原则

- 原始快照和历史证据尽量追加保存，不因目录重建直接物理删除。
- 展示层聚合与底层历史存储分离：页面可以只展示一条，数据库仍保留可追溯轨迹。
- 可恢复的局部失败优先降级或隔离；只有数据库损坏、协议不兼容、事务无法保证等硬错误才阻断发布。
- 所有同步操作必须幂等；同一 bundle 重复执行不得造成重复记录或状态漂移。
- 优先完成止损和正确性，再进行性能重构和体验优化。

## 3. 第一阶段：数据安全、聚合口径与采集可靠性

第一阶段是当前最高优先级，完成前不建议继续大规模增加新渠道。

### 3.1 产品与渠道聚合口径

#### 3.1.1 产品聚合视图

产品聚合不再把事件类型作为聚合键。

过渡期聚合键：

```text
normalized_product_name
```

稳定产品 ID 上线后的聚合键：

```text
game_uuid
```

规则：

1. 同一归一化产品名下的预约、招募、测试、预下载、首发、上线等事件，在产品列表中只生成一张产品卡片。
2. 事件类型、事件日期和来源渠道作为产品内部的事件轨迹保留，不参与产品卡片去重。
3. 日期筛选先确定命中筛选范围的事件，再按产品聚合；同一产品在同一日命中多个事件时仍只计为一个产品。
4. “全部”范围下每个产品仍只出现一次；详情页展示完整渠道和事件轨迹。
5. 卡片代表事件按以下顺序选择：最近的未来有效事件；若没有未来事件，则选择最近已经发生的有效事件；同日时优先选择最近采集确认的记录。
6. 产品名称使用清理活动后缀后的标准展示名，原始渠道名称继续保存在渠道记录中。

#### 3.1.2 渠道明细视图

渠道明细不再把事件类型作为聚合键。

过渡期聚合键：

```text
normalized_product_name + source
```

稳定产品 ID 上线后的聚合键：

```text
game_uuid + source
```

规则：

1. 同一产品在同一渠道出现预约、测试、首发等多个事件时，渠道列表只生成一张卡片。
2. 不同渠道仍分别展示，例如同一产品在 OPPO 和荣耀各有一条渠道卡片。
3. 渠道卡片内部保留该渠道全部事件轨迹，不能因聚合而删除历史事件。
4. 日期筛选范围内，同一产品同一渠道无论命中多少事件，都只计为一个渠道明细。
5. API 必须返回稳定的 `group_key`、组内事件数量和事件列表，避免前端再次自行拼接去重逻辑。

#### 3.1.3 聚合口径验收样例

| 输入 | 产品聚合预期 | 渠道明细预期 |
|---|---|---|
| `头号禁区`、`头号禁区（限时开测）` | 1 个产品 | 每个实际来源渠道各 1 条 |
| 同一 OPPO 产品同时存在测试、预约、首发 | 1 个产品 | OPPO 仅 1 条，详情保留三个事件 |
| 同一产品同时存在 OPPO、荣耀记录 | 1 个产品 | 2 条渠道明细 |
| `绝境猎人` 同渠道出现重复事件文案 | 1 个产品 | 该渠道 1 条，使用最新状态并保留历史证据 |
| 同名但包名、开发商和渠道 ID 均不同 | 不得仅凭名称强制合并 | 分别保留并进入身份复核 |

### 3.2 第一阶段任务清单

| ID | 任务 | 优先级 | 状态 | 依赖 | 核心验收标准 |
|---|---|---:|---|---|---|
| S1-01 | 产品视图改为仅按产品聚合，不使用事件类型作为聚合键 | P0 | 待验收 | 无 | 同一产品多个事件类型只返回一张产品卡；详情事件不丢失 |
| S1-02 | 渠道视图改为按“产品＋渠道”聚合，不使用事件类型作为聚合键 | P0 | 待验收 | S1-01 | 同一产品同一渠道只返回一条；不同渠道分别保留 |
| S1-03 | 为两种视图定义统一 `group_key`、代表事件选择和统计口径 | P0 | 待验收 | S1-01、S1-02 | `/api/games`、summary、calendar、前端数量完全一致 |
| S1-04 | 增加聚合回归测试和真实重复样本夹具 | P0 | 待验收 | S1-01、S1-02 | 覆盖头号禁区、绝境猎人、改期、同日多事件、多渠道和同名不同产品 |
| S1-05 | 将数据库、媒体和 raw 迁出 release 目录 | P0 | 待验收 | 部署窗口 | 发布期间新增关注、账号、API Key 不丢失 |
| S1-06 | 使用 SQLite Backup API 或 `VACUUM INTO` 生成一致快照 | P0 | 待验收 | S1-05 | 不再直接复制活跃 `.db`；快照通过 `quick_check` |
| S1-07 | 同步导入改为 staging＋单事务＋失败回滚 | P0 | 待验收 | S1-05 | 目录重建故障注入后 DB 和正式媒体目录均无变化 |
| S1-08 | 增加 `updated_at`、变更日志/outbox、删除 tombstone 和 bundle 游标 | P0 | 待验收 | S1-07 | 详情补全、历史修复和删除均可同步；重复消费幂等 |
| S1-09 | 实现部分成功发布和产品隔离 | P0 | 待验收 | S1-07 | 成功渠道正常发布；失败渠道沿用旧数据；冲突产品不阻断其他产品 |
| S1-10 | 引入 pipeline run、stage、bundle ID 和服务端发布回执 | P1 | 待验收 | S1-07 | 可追踪 collected、validated、imported、published、partial、failed |
| S1-11 | 修复 vivo `hasNext` 翻页 | P0 | 待验收 | 无 | 采集到最后一页并记录页数；未完成翻页不得标记完整成功 |
| S1-12 | 华为只读取本轮新增或最新缓存，并记录缓存年龄 | P0 | 待验收 | 无 | 刷新失败显示 degraded；不能把任意旧缓存当成本轮成功 |
| S1-13 | 荣耀、OPPO 增加到底证明和 `truncated/missing` 状态 | P0 | 待验收 | 无 | 撞 8/9 屏上限不得标记 complete；分区缺失必须告警 |
| S1-14 | 为 HTTP、ADB 和单渠道任务增加分级重试、退避和超时 | P1 | 待验收 | 无 | 瞬时 405/5xx/超时自动重试；确定性解析冲突不盲目重试 |
| S1-15 | 增加动态数量基线、页面契约和真实站点冒烟测试 | P1 | 待验收 | S1-11～S1-14 | 数量骤降、页面结构变化和连续失败能够自动告警 |
| S1-16 | 修复已知字段更新错误 | P1 | 待验收 | 无 | 空值不再覆盖已有 `event_end_time`；补充相应回归测试 |
| S1-17 | 健康检查拆分为 `/livez`、`/readyz` 和内部运行详情 | P1 | 待验收 | S1-10 | 必需渠道失败或数据过期时 readyz 返回 503；公开响应脱敏 |
| S1-18 | 第一阶段生产灰度与回滚演练 | P0 | 未开始 | S1-01～S1-17 | 连续 7 天运行无数据丢失、无半导入、无错误聚合；完成回滚演练 |

### 3.2.1 本轮实现与验证结果

- S1-01～S1-17 已完成代码、自动化测试和本地故障注入，统一标记为“待验收”，需部署后做生产数据核对。
- 全量自动化回归通过；覆盖部分渠道失败发布、产品冲突隔离、历史详情更新、tombstone、bundle 幂等和 DB/媒体回滚。
- 2026-08-28 真实站点只读冒烟通过：TapTap 5 条、Vivo 首页 20 条且 `hasNext=true`、九游 46 条。
- S1-18 未标记完成：连续 7 天生产灰度是时间型验收条件，需在部署后按日回填证据。

### 3.3 第一阶段完成定义

第一阶段只有同时满足以下条件才可标记完成：

- 任意一个渠道失败时，其他成功渠道仍可发布。
- 任意一个产品归一化冲突时，仅该产品被隔离。
- 产品视图不再因事件类型产生重复产品卡片。
- 渠道视图不再因事件类型产生同产品同渠道重复卡片。
- 目录重建失败不会在服务端留下新增来源记录、运行记录或半成品媒体。
- 详情补全、历史修复和删除可以通过增量协议完整同步。
- 发布期间进行关注、创建 API Key、修改账号，切换后数据仍存在。
- vivo 完成真实翻页；华为能证明缓存新鲜；荣耀和 OPPO 能报告是否截断。
- TapTap、九游等瞬时失败具备自动重试和数量异常检测。
- 连续 7 天生产灰度期间无严重回归。

## 4. 第二阶段：稳定身份、事件模型与安全边界

### 4.1 目标

将当前以名称为核心的来源事件表，逐步迁移到稳定产品身份和可追溯快照模型。

目标结构：

```text
canonical_game
├─ game_alias
├─ identity_evidence
├─ platform_listing
│  └─ listing_snapshot
├─ release_event
│  └─ event_schedule_version
├─ test_round
└─ media_asset
```

### 4.2 第二阶段任务清单

| ID | 任务 | 优先级 | 状态 | 依赖 | 核心验收标准 |
|---|---|---:|---|---|---|
| S2-01 | 引入不可变 `game_uuid`，收藏和 API 改用稳定 ID | P0 | 已完成 | 第一阶段 | 改名、合并后收藏不迁移也不丢失 |
| S2-02 | 建立渠道 listing、别名和身份佐证表 | P1 | 已完成 | S2-01 | 同渠道 ID、包名族、开发商、详情 URL 可追溯 |
| S2-03 | 名称归一化改为候选召回，不直接决定产品身份 | P1 | 已完成 | S2-02 | 同名不同产品不自动合并；低置信度进入复核队列 |
| S2-04 | 拆分 listing snapshot、release event 和 test round | P1 | 已完成 | S2-01 | 可区分改期、多轮测试、取消、下架和重新上线 |
| S2-05 | 增加 `announced_at/scheduled_at/actual_at` 及计划版本历史 | P1 | 已完成 | S2-04 | 跳票不覆盖旧计划；日期来源和精度可解释 |
| S2-06 | 统一原始事件类型和受控事件类型 | P1 | 已完成 | S2-04 | 未知类型进入 unknown/quarantine，不再被页面静默隐藏 |
| S2-07 | 建立详情、图集和身份复核队列 | P1 | 已完成 | S2-02 | 每个缺口有状态、重试次数、证据和最终处理结果 |
| S2-08 | 建立统一安全下载器 | P0 | 已完成 | 无 | 拒绝 SSRF、内网地址、超大响应和超大像素；限制可信 CDN |
| S2-09 | 数据迁移脚本、双读校验和回滚方案 | P0 | 已完成 | S2-01～S2-06 | 新旧模型统计一致，可一键回退 |

### 4.2.1 本轮实现与验证结果

- 稳定身份：677 个现有产品在真实数据库副本中全部获得 `game_uuid`；产品改名继续沿用 UUID，合并使用 UUID 重定向，旧数字 ID 和旧 `game_key` 保持兼容。
- 身份佐证：生成 958 个渠道 listing，并记录名称别名、渠道 ID、包名族、开发商和详情 URL 证据；新发现的同名强冲突不再自动合并，历史冲突进入身份拆分复核。
- 事件模型：1075 条来源记录生成 1075 个 listing snapshot 和 1075 个计划版本，聚合为 1041 个 release event；测试轮次、原始类型和受控类型独立保存。
- 受控事件：当前真实数据的原始事件类型均已映射，unknown 待处理数为 0；未来未知类型会进入 quarantine，并以“待分类事件”显示而非静默隐藏。
- 复核工作流：详情、图集、身份三类队列支持状态、优先级、领取、重试次数、下次重试、证据和处理结果；管理员 API 可查询和处理。
- 安全下载：图标和图集统一经过可信域名、DNS 公网地址、端口、重定向、内容类型、响应体积、图片尺寸和总像素检查；当前 16 个实际媒体域名全部在明确清单内。
- 迁移回滚：`scripts/migrate_phase2.py` 支持 apply/audit/rollback；真实数据库副本迁移审计为 `ok`，回滚后 1075 条来源记录、677 个产品及 SQLite `quick_check` 均一致。
- 自动化验证：135 项测试全部通过；Python compileall、前端 JavaScript 语法检查和 `git diff --check` 通过。

### 4.3 第二阶段实施拆解

| 批次 | 覆盖任务 | 主要交付物 | 验收门槛 |
|---|---|---|---|
| A：稳定产品身份 | S2-01～S2-03 | `game_uuid`、渠道 listing、别名、身份佐证、候选召回和复核隔离 | 改名/合并后 UUID 与收藏不变；同名异产品不误并 |
| B：事件与复核工作流 | S2-04～S2-07 | listing snapshot、release event、test round、计划版本、受控事件类型、详情/图集/身份复核队列 | 改期保留历史，多轮测试可区分，未知事件不丢失，缺口可重试可审计 |
| C：安全与迁移 | S2-08～S2-09 | 统一安全下载器、迁移命令、双读一致性报告、数据库快照与回滚命令 | SSRF/超限资源被拒绝；新旧统计一致；故障注入可恢复 |

实施约束：每批均先补迁移与测试，再接入在线读写；旧数字 ID 和旧 `game_key` 在第二阶段保持兼容，不做破坏性删除。

## 5. 第三阶段：性能、工程化与长期运维

| ID | 任务 | 优先级 | 状态 | 依赖 | 核心验收标准 |
|---|---|---:|---|---|---|
| S3-01 | Schema migration 从每次连接移到独立迁移流程 | P1 | 已完成 | 第二阶段 | Web 请求不再执行 DDL |
| S3-02 | 配置 WAL、busy timeout、读写重试和只读连接 | P1 | 已完成 | S3-01 | 采集导入期间 Web 读请求稳定，写冲突有界重试 |
| S3-03 | 筛选、排序、分页和统计下推 SQL | P1 | 未开始 | 第二阶段 | 10 万事件规模下核心 API P95 小于 500ms |
| S3-04 | 增加目录 generation 缓存和统计预聚合 | P2 | 未开始 | S3-03 | summary 不再重复全库构建 |
| S3-05 | 增加 `pyproject.toml`、依赖锁、开发依赖和 SBOM | P1 | 未开始 | 无 | 新环境可复现安装和测试 |
| S3-06 | 建立 CI：测试、覆盖率、lint、类型、依赖和安全扫描 | P1 | 未开始 | S3-05 | 主分支合并前自动门禁 |
| S3-07 | 版本化完整 Web、采集和模拟器部署配置 | P1 | 未开始 | 第一阶段 | 生产逻辑不再依赖 Git 忽略脚本 |
| S3-08 | 发布制品校验、自动回滚和数据库备份恢复演练 | P1 | 待验收 | S3-07 | readyz 失败自动切回旧版本；恢复演练通过 |
| S3-09 | raw、日志、workspace、bundle、release 和媒体保留策略 | P1 | 未开始 | S3-07 | 有天数、容量和失败现场上限；不会无限增长 |
| S3-10 | 安全头、生产 Cookie 策略、API Key scope/过期和审计日志 | P1 | 未开始 | 无 | 生产未启用 Secure Cookie 时拒绝启动；敏感操作可追溯 |
| S3-11 | 拆分大型模块和统一配置对象 | P2 | 未开始 | 第二阶段 | 采集器、数据层、pipeline、Web 层职责清晰 |
| S3-12 | 数据质量与渠道覆盖监控看板 | P1 | 未开始 | S1-10、S2-07 | 展示新鲜度、完整度、异常数量、截断和详情缺口趋势 |

### 5.1 第三阶段实施拆解

| 批次 | 覆盖任务 | 主要交付物 | 当前状态 |
|---|---|---|---|
| A：数据库与发布底座 | S3-01、S3-02、S3-08 | 独立迁移、WAL/锁等待/只读连接、制品校验、启动等待、基线感知回滚 | S3-01/02 已完成，S3-08 待自动回切演练 |
| B：工程化门禁 | S3-05、S3-06、S3-07 | 可复现依赖、SBOM、CI 门禁、完整版本化部署配置 | 未开始 |
| C：查询性能 | S3-03、S3-04 | SQL 下推、基准压测、generation 缓存、统计预聚合 | 未开始 |
| D：长期运维 | S3-09～S3-12 | 保留策略、安全审计、模块拆分、数据质量与覆盖看板 | 未开始 |

首批调整原因：第二阶段上线时实测发现 Web 进程需要启动等待，且生产 `readyz` 存在部署前已知的 TapTap 故障；第三阶段先完善“制品校验 + 发布前后就绪基线比较”，避免把既有渠道故障误判为新版本回归。同时优先移除请求路径中的 DDL，降低采集写入与 Web 查询的锁竞争。

## 6. 基线指标

以下指标用于比较优化前后效果，后续每次阶段验收时更新。

| 指标 | 2026-08-28 基线 | 目标 |
|---|---:|---:|
| 自动化测试 | 优化前 107 tests、43 subtests 通过 | 第一阶段 121 tests；第二阶段 135 tests，覆盖稳定身份、事件版本、安全下载和迁移回滚 |
| 渠道事件记录 | 1075 | 保持可追溯，不因聚合物理丢失 |
| 归一化产品 | 677 | 使用稳定 ID 后重新建立基线 |
| 开发商覆盖率 | 84.9% | ≥ 90% |
| 有效简介覆盖率 | 86.1% | ≥ 95% |
| 长介绍覆盖率 | 73.0% | ≥ 90% |
| 产品图集覆盖率 | 95.9% | ≥ 98%，并按渠道分别监控 |
| 包名覆盖率 | 58.2% | ≥ 75%，无法提供的渠道单独说明 |
| 明确日期不一致 | 15 条，其中 2 条高风险 | 高风险为 0，其余有复核状态 |
| `/api/summary` 响应 | 约 1.5 秒 | 当前规模 P95 < 300ms；10 万事件 P95 < 500ms |
| 部分失败发布 | 不支持 | 支持，失败渠道保留旧数据 |
| 采集完整度状态 | success/failed | complete/degraded/truncated/missing/failed |

## 7. 进度汇总

| 阶段 | 任务数 | 已完成 | 进行中 | 阻塞 | 待验收 | 未开始 | 阶段状态 |
|---|---:|---:|---:|---:|---:|---:|---|
| 第一阶段 | 18 | 0 | 0 | 0 | 17 | 1 | 实现完成，待生产灰度 |
| 第二阶段 | 9 | 9 | 0 | 0 | 0 | 0 | 已部署并通过生产验收 |
| 第三阶段 | 12 | 2 | 0 | 0 | 1 | 9 | A 批剩余自动回切演练 |

## 8. 任务更新记录

每次优化提交、部署或验收后追加一行，不覆盖历史记录。

| 日期 | 任务 ID | 状态变化 | 提交/版本 | 验收证据 | 备注 |
|---|---|---|---|---|---|
| 2026-08-28 | 规划文档 | 新建 | - | 完成项目 Review 并形成三阶段计划 | 第一阶段加入产品及渠道聚合口径优化 |
| 2026-08-28 | S1-01～S1-17 | 未开始 → 待验收 | 待提交 | 121 项自动化测试；TapTap/Vivo/九游真实站点冒烟通过 | 已完成本地实现；生产部署、7 天灰度与回滚演练纳入 S1-18 |
| 2026-08-28 | S2-01 | 未开始 → 进行中 | - | 第二阶段基线 121 项测试通过 | 已拆为稳定身份、事件工作流、安全迁移三个批次，先实施 A 批 |
| 2026-08-28 | S2-01～S2-09 | 进行中/未开始 → 待验收 | 待提交 | 135 项测试；真实库副本 677 UUID、958 listing、1075 snapshot/version；迁移回滚通过 | 本地实现完成，生产迁移和灰度前不标记已完成 |
| 2026-08-28 | S2-01～S2-09 | 待验收 → 已完成 | `1c30188` / `20260828_130545_1c30188` | 生产审计 703 UUID、991 listing、1124 snapshot/version、0 issue；`quick_check=ok`；UUID 详情与产品聚合通过 | `readyz` 仅保留发布前既有 `taptap: failed`，无新增故障 |
| 2026-08-28 | S3-01、S3-02、S3-08 | 未开始 → 待验收/进行中 | `78671db` | 143 tests、43 subtests；真实库副本快照/迁移审计 `ok`；制品篡改被拒绝；compileall、Shell 语法和 diff check 通过 | S3-01、S3-02 待生产验收；S3-08 生产发布与回滚验证中 |
| 2026-08-28 | S3-01、S3-02、S3-08 | 待验收/进行中 → 已完成/待验收 | `c1dbee8` / `20260828_134026_c1dbee8` | Schema v3、WAL、5000ms busy timeout、只读写拒绝；模型审计 0 issue；一致快照隔离恢复 703 产品/1124 来源记录；发布前后仅 `taptap: failed` | S3-01、S3-02 完成；S3-08 的制品校验、启动等待、基线比较和恢复演练通过，尚缺自动回切故障演练 |
| 2026-09-01 | S1-09、S1-13、S1-14、S3-07 | 可靠性修复 | 待提交 | 146 tests、43 subtests；PowerShell 脚本语法通过；09-01 保留 bundle 已远端入库 326 条 | TapTap、九游调整为本机网络采集，服务器移除重复/反爬失败任务；荣耀增加无文字推荐弹窗关闭；九游纳入可选增量同步 |

## 9. 单项任务更新模板

后续更新任务时复制以下模板：

```markdown
### S1-XX 任务名称

- 状态：未开始 / 进行中 / 阻塞 / 待验收 / 已完成
- 负责人：
- 开始日期：
- 目标日期：
- 关联提交：
- 变更范围：
- 数据迁移：无 / 有，说明回滚方式
- 自动化测试：
- 手工验收：
- 生产观察：
- 风险与阻塞：
- 最终结论：
```

## 10. 当前下一步

1. 完成 S3-08：在隔离服务端口/临时 current 链接下演练存活超时和新增 readyz 故障，验证自动回切，不对生产流量执行故障注入。
2. A 批验收后启动 B 批 S3-05～S3-07：可复现依赖、SBOM、CI 门禁和完整版本化部署配置。
3. 继续按日回填 S1-18 的 7 天生产灰度证据，并确认下一次每日采集在 WAL 模式下 Web 查询稳定。
